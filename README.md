# 云起慧播 2.0 UI

云起慧播 2.0 桌面端 UI 原型，包含登录、注册、AI 直播工作台、助手配置、直播复盘、素材中心和直播设备设置等页面。

## 本地运行

需要安装 Node.js 和 pnpm。

```bash
pnpm install
pnpm run dev
```

启动后访问终端中显示的本地地址。

## 构建

```bash
pnpm run build
```

构建产物将生成在 `dist` 目录。

